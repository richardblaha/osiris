{
  "name": "@osiris/lm-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "An OpenAI-compatible HTTP shim over an editor Language Model API (VS Code / Copilot Chat), so a container-side agent gets its models from the editor.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@osiris/shared-core": "workspace:*"
  },
  "devDependencies": {
    "@osiris/agent-core": "workspace:*",
    "@osiris/tsconfig": "workspace:*",
    "@types/node": "^26.4.0",
    "eslint": "^10.9.1",
    "rimraf": "^6.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
