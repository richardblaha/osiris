---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: osirissessions.osiris.osiris.dev
spec:
  group: osiris.osiris.dev
  names:
    kind: OsirisSession
    listKind: OsirisSessionList
    plural: osirissessions
    shortNames:
      - osess
    singular: osirissession
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.projectRef
          name: Project
          type: string
        - jsonPath: .spec.desiredPhase
          name: Desired
          type: string
        - jsonPath: .status.phase
          name: Phase
          type: string
        - jsonPath: .status.lastActivityAt
          name: LastActivity
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: OsirisSession is the Schema for the osirissessions API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: OsirisSessionSpec defines the desired state of OsirisSession.
              properties:
                desiredPhase:
                  default: Running
                  description: |-
                    desiredPhase is what the client wants. Idle-timeout auto-suspend does
                    NOT flip this field — it stays Running to represent "the user hasn't
                    asked to suspend", distinct from the controller's own idle-driven
                    scale-to-0 (see status.phase for the observed state).
                  enum:
                    - Running
                    - Suspended
                  type: string
                idleTimeoutOverrideSeconds:
                  description: |-
                    idleTimeoutOverrideSeconds overrides the project/global default for
                    this session only.
                  format: int32
                  type: integer
                projectRef:
                  description: |-
                    projectRef names the OsirisProject in the same namespace this session
                    belongs to. Immutable after creation.
                  type: string
                  x-kubernetes-validations:
                    - message: projectRef is immutable
                      rule: self == oldSelf
                workspaceSize:
                  description: |-
                    workspaceSize for this session's PVC; defaults to the project's
                    defaultWorkspaceSize if unset.
                  type: string
              required:
                - projectRef
              type: object
            status:
              description: OsirisSessionStatus defines the observed state of OsirisSession.
              properties:
                conditions:
                  items:
                    description: Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                effectiveIdleTimeoutSeconds:
                  description: |-
                    effectiveIdleTimeoutSeconds is the resolved timeout (session override ->
                    project default -> operator flag default) as of the last reconcile.
                  format: int32
                  type: integer
                lastActivityAt:
                  description: |-
                    lastActivityAt mirrors the activity Lease's renewTime for this session,
                    kept here purely for kubectl get/describe visibility. The controller's
                    own idle-timeout math reads the Lease directly, not this field.
                  format: date-time
                  type: string
                leaseRef:
                  type: string
                observedGeneration:
                  format: int64
                  type: integer
                phase:
                  description: phase is the controller-observed session state.
                  enum:
                    - Pending
                    - Running
                    - Suspending
                    - Suspended
                    - Resuming
                    - Terminating
                  type: string
                pvcRef:
                  type: string
                replicas:
                  description: replicas mirrors the underlying Deployment's observed
                    replica count (0 or 1).
                  format: int32
                  type: integer
                workloadRef:
                  description: workloadRef/pvcRef/leaseRef name the owned child objects.
                  type: string
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
